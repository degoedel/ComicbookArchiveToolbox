<UserControl x:Class="ComicbookArchiveToolbox.Module.Edit.Views.EditPluginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
	<Border Padding="32" Margin="24" CornerRadius="12">
		<StackPanel>
			<!-- Title -->
			<TextBlock Text="Edit Metadata" FontSize="28" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,24"/>

			<!-- File Selection -->
			<GroupBox Header="File to Edit" Margin="0,0,0,16">
				<StackPanel>
					<TextBlock Text="Select the archive or file to edit:" Margin="0,0,0,4"/>
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition/>
							<ColumnDefinition Width="Auto"/>
						</Grid.ColumnDefinitions>
						<TextBox Grid.Column="0" Height="32" Text="{Binding FileToEdit, Mode=TwoWay}" VerticalContentAlignment="Center"/>
						<Button Grid.Column="1" Content="Browse..." Margin="8,0,0,0" Height="32" Command="{Binding BrowseFileCommand}"/>
					</Grid>
				</StackPanel>
			</GroupBox>

			<!-- Metadata Editing -->
			<GroupBox Header="Metadata" Margin="0,0,0,16">
				<DataGrid ItemsSource="{Binding MetadataCollection}"
                          CanUserDeleteRows="True"
                          CanUserAddRows="True"
                          AutoGenerateColumns="True"
                          Margin="0"
                          MinHeight="180"
                          MaxHeight="300"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Visible"/>
			</GroupBox>

			<!-- Action Button -->
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
				<Button Content="Save" Width="120" Height="40" Command="{Binding SaveCommand}"/>
			</StackPanel>
		</StackPanel>
	</Border>
</UserControl>
